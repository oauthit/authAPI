.container
  .buttons
    button.pull-right.btn.btn-primary(type='button' ng-disabled='vm.disableAddContact()' ui-sref='debt.contact.add')
      | Add contact
  h1 My contacts

  table.table
    thead
    th Name
    tbody
      tr(ng-repeat='contact in vm.contacts track by contact.id')
        td {{contact.counterAgent.name}}
